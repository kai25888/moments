# 2026-04-14

## 今日目标
- 为 `moments` 的 fork 建立正式长期分支、独立标签空间和可直接用于 NAS 的镜像发布链路。
- 沉淀今天的长期记忆与每日日志规范，并准备固化为可复用 skill。

## 今日完成
- 将长期正式开发分支确定为 `moments-plus/main`。
- 删除了临时命名风格的旧远程分支 `feature/nas-dev`。
- 为 fork 保留上游原有 `v*` 标签体系，不做覆盖或替换。
- 创建了 fork 自己的命名空间标签 `moments-plus/v0.0.1`。
- 新增并启用了分支镜像工作流：
  - 触发分支：`moments-plus/main`
  - 主要镜像标签：`ghcr.io/kai25888/moments:moments-plus-main`
- 新增并启用了命名空间标签镜像工作流：
  - 触发标签：`moments-plus/v*`
  - 主要镜像标签：
    - `ghcr.io/kai25888/moments:moments-plus-v0.0.1`
    - `ghcr.io/kai25888/moments:moments-plus-latest`
- 已触发 `moments-plus/v0.0.1` 对应的 GitHub Actions 镜像构建。
- 将镜像命名策略从“共用 `moments` 镜像仓库 + 前缀 tag”调整为“每条长期分支线使用独立 GHCR 包名”：
  - `moments-plus/main` -> `ghcr.io/kai25888/moments-plus:latest`
  - `moments-plus/v*` -> `ghcr.io/kai25888/moments-plus:vX.Y.Z`
  - `tech-standards/main` -> `ghcr.io/kai25888/tech-standards:latest`
  - `tech-standards/*` -> `ghcr.io/kai25888/tech-standards:vX.Y.Z`

## 关键决策
- 分支命名采用“主题/产品线 + main”的长期风格，参考 `tech-standards/main`，不再用 `feature/*` 作为主开发线。
- 发布体系与上游隔离：
  - 上游继续使用 `v*`
  - fork 使用 `moments-plus/v*`
- 镜像优先走 `GHCR`，减少对上游 DockerHub secrets 和既有发布策略的依赖。
- 镜像命名采用“分支线 = 独立镜像名”的方式，避免继续使用 `ghcr.io/.../moments:<line-tag>` 这种二级表达。

## 遇到的问题
- 本机多次执行 `git push` 时遇到 GitHub 传输层异常，包括：
  - HTTP/2 framing layer 错误
  - 443 连接超时
- 为避免阻塞进度，部分远程变更改用 GitHub API 直接完成，包括：
  - 创建和调整远程分支引用
  - 创建和重建命名空间标签
  - 将新的工作流文件直接提交到远程分支

## 当前状态
- 远程分支 `moments-plus/main` 已生效并承担正式开发职责。
- 远程标签 `moments-plus/v0.0.1` 已生效并指向包含标签镜像工作流的提交。
- 由于远程部分提交是通过 GitHub API 创建，本地分支暂时显示与远程存在一条待自然对齐的提交历史差异；远程内容本身已经正确生效。

## 安全提醒
- 今天在沟通中暴露过一个 GitHub PAT。
- 该 token 虽未写入仓库，但仍应尽快在 GitHub 中作废并重新生成。

## 下一步建议
- 将本地分支与远程提交历史重新对齐。
- 继续推进剩余高优改造：
  - 管理员权限角色化
  - JWT 过期与 claims 校验
  - 非 root 容器运行
  - 前端路由级鉴权
